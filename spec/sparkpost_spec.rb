require 'spec_helper'

describe Sparkpost do
  it 'has a version number' do
    expect(Sparkpost::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
